class Video {
  constructor(id, title, durationSeconds, url) {
    this.id = id;
    this.title = title;
    this.durationSeconds = durationSeconds;
    this.url = url;
  }
}

module.exports = Video;
